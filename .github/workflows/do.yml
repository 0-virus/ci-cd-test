name: Github Actions 실행하기

on:
  push:
    branches:
      - main

jobs:
  My-First-CICD-Job:
    runs-on: ubuntu-latest
    steps:
      - name: sample-web
        run: |
          pwd
          ls -al
          echo "This is the first cicd step" >> a.txt
          cat a.txt
